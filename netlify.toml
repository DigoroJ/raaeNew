[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  CI = "false"

# ✅ React Router fix (VERY IMPORTANT)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200